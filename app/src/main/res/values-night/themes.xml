<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Prevent dark mode from changing colors -->
    <style name="Base.Theme.WiFiManager" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/topColor</item>
        <item name="android:forceDarkAllowed" tools:ignore="NewApi">false</item>
    </style>

    <style name="Theme.WiFiManager" parent="Base.Theme.WiFiManager" />
</resources>
